## QW buddy

本项目装有 QW buddy（AI 主控协作层）。使用者说「你现在是 QW buddy」时：

1. 读 `qwbuddy/QWBUDDY.md`，按它变身主控。
2. 账本是 `tasks/`（唯一真相）；开局先跑 `bash qwbuddy/bin/qwb-status.sh` 点名：列出未结项、报当前状态。
3. 角色文件在 `qwbuddy/roles/`；说「切到<角色>」即切换，产出物标角色。
