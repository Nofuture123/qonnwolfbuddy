# 审核任务书：第四轮聚焦复核（H1/H2/H3 修复）

```
审核 id:   qwbuddy-round4-audit
state:     verified
审核者:    gpt-6-astra / medium（Codex CLI，交互式 Herdr 窗口，-s danger-full-access -a never）
送审时间:  2026-09-15
产出:      docs/reviews/2026-09-15-第四轮复核.md（本轮唯一允许写入的文件）
```

## 你的身份与限制

你是**审核者**（见 `templates/roles/审核者.md`）：只挑错、不提方案、不改代码、区分「必须补」与「可以不要」、**最多 3 个审点**、只报有证据的真问题、**只读**（除产出文件）。

## 背景

上一轮（`docs/reviews/2026-09-15-统一复核.md`）判不通过：U1 TOCTOU（致命）、U2 idle 忙循环（必须补）、U3 字符串拼接躲门（可以不要）。三条已返修，提交 `1cb6217`，主控验收记录见 `tasks/2026-09-15-qwbuddy返修-统一复核三发现.md`。

**本轮范围刻意收窄**：只复核这次修复本身是否正确、有无新洞。**不要重审**已被确认无问题的部分（F1–F4、G1 非并发路径、G3 配置来源、G4 `--keep`）——若你在复核中**顺带**发现它们仍有问题，可以报，但要说明是顺带发现。

## 送审对象

- **变更**：`git show 1cb6217`（或 `git diff <1cb6217^> 1cb6217`）—— 就是本轮要审的东西
- `bin/qwb-worktree.sh`（H1）、`bin/qwb-wake.sh`（H2）、`bin/qwb-init.sh`（H3）、`tests/smoke.sh`
- 参考：`docs/DESIGN.md` §3.2（TOCTOU 规则）、§4.3（等待预算）

## 三个审点

### 审点 1：H1 的删前复核是否真的封住了 TOCTOU，且没误伤正常路径

- `check_unchanged` / `recheck_head` 是否插在**每一个**破坏性动作之前（`worktree remove`、`branch -d`、`branch -D`、以及 `--merged` 判断通过之后）？
- 有没有**遗漏的破坏性路径**（例如 `--archive` 打标签本身、`list` 之外的其它子命令、异常分支）？
- 复核之后到真正执行删除之间，**仍存在窗口吗**（复核与删除之间是否还有可被推进的间隙）？如果存在，请明确它还剩多大、需要多大的并发才触发。
- **反向**：正常路径（无并发）会不会被**误拒**？特别是 attached 分支、标签已存在、worktree 目录名 ≠ 分支名这些正常场景。
- 复核失败时是否**保证什么都没删**（包括不删分支、不清理标签）？

### 审点 2：H2 的等待预算是否既消除忙循环、又没引入新问题

- 改造后 `wait_round` 在三种返回下（立即成功 / 立即失败 / 耗尽 timeout）各自的**单轮耗时**是多少？请用你上一轮同样的内存替身方法实测三轮总耗时。
- 有没有**新的退化路径**：例如 `date +%s` 秒级精度导致 `dt*1000 < INTERVAL` 判断在极短 interval（如 100）下出错？`sleep` 取整是否会让一轮远超预算？interval 极大时会不会睡太久？
- `herdr agent wait ... || true` 的写法是否会让**真实错误被吞掉**（例如 pane 不存在时的错误信息不再可见）？这与「失败必须留痕」是否冲突？

### 审点 3：H3 的大门的修复是否到位

- `bin/qwb-init.sh` 的旧名迁移提示是否**功能完好**（没有为了「去掉拼接」而把功能也删掉）？
- `tests/smoke.sh` 里改后的断言是否**真的能挡住**拼接类规避（试想：`"config.jso"+"n"`、`$'config.json'`、变量拼接等变体，新断言能挡住哪些、挡不住哪些）？
- 本轮**有没有出现新的「为过门而写」的痕迹**（任何测试或实现里为迎合断言而非实现需求的写法）？

## 要求

- 证据具体到文件与行号，或给出可复核的最小复现（内存替身可以，但标明是替身）。
- 没问题就说没问题，并说明你实际验证了什么。不要凑数。
- 不要提出新功能需求。

## 产出格式

写入 `docs/reviews/2026-09-15-第四轮复核.md`：

```
# 第四轮聚焦复核：H1/H2/H3 修复
审核对象：<commit sha>
审核者：gpt-6-astra / medium
证据边界：<怎么验的、哪些是替身、哪些没验>
## 审点 1：H1 删前复核
## 审点 2：H2 等待预算
## 审点 3：H3 门与迁移提示
## 结论
通过 / 不通过（列致命项）
```

最后一行打印 `AUDIT DONE <致命数>/<必须补数>/<可以不要数>`。
