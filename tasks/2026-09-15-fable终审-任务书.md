# 终审任务书：QW buddy 场景完善三票后的最终整体审核

```
审核 id:   qwbuddy-final-fable
state:     running
审核者:    claude-fable-5.1 / low（Claude Code CLI，同上轮原模型）
产出:      docs/reviews/2026-09-15-fable-终审.md（唯一允许写入的文件）
身份:      审核者——只挑错、给判定，不改代码、不递归派工
```

## 一、项目目标（三句话，不变）

QW buddy 是**装进单个项目**的「AI 主控协作层」：说明书 + shell 脚本 + markdown 账本。管的不是"一个 AI 怎么干好活"，而是"**活怎么被派发、验证、记住**"。三条尺子：① 谁的状态归谁；② 薄即抗淘汰；③ 能自动检查的规范不写在纸上。硬边界：零通知使用者、无队列/数据库/守护进程、只用 Herdr、纯 shell 零依赖。

## 二、你上一轮说了什么（2026-09-15-fable-评审.md）

总结论「**产品够了，流程过头了**」；两条必须：① 先跑真实闭环再冻结新机制；② 状态行解析收敛到一处并识别代码围栏。第二轮场景需求评估：10 条满足 4 / 部分 4 / 不满足 2，不满足的两条都是"没在真实项目用过"。

## 三、此后发生了什么（待你核验）

- **三票场景完善已落地并验收合并**（`tasks/2026-09-15-场景完善*.md` 四份 + `docs/E2E-RUNBOOK.md`）：
  - **01 真实闭环**：真实 Herdr + 真实 Devin 工人 + 真值守 + 主控被真实唤醒做独立验收收尾，全链路走通；首次 trust 对话框吞提示词记为真实缺陷（人工介入 2 次登记，原始路径不算无干预 PASS）。
  - **02 值守生命周期**：`qwb-wake.sh --ensure/--check`（幂等值守、目标 pane 核验、ws 边界、混合态 fail-closed、SIGTERM 清锁 trap）+ `qwb-status.sh` 值守段 + `qwb-run.sh --pane` cwd 预检。三轮返修 + Opus 复审收敛 + 主控真实 Herdr 反例复验（11 live + SIGTERM + 目标 pane 零副作用）。
  - **03 审核身份**：lint 第 7 节，`review-required: yes` 票核对 model/family/session/evidence；同 CLI 不同 family 合法、不同 CLI 同 family 拒绝、unknown 不假绿；主控 12 项反例 + 反转验证。
- 两条必须的处置：**① 已做**（01 票）；**② 未做**（状态行解析收敛 + 围栏——02 票登记为"围栏解析需单独设计，本轮不实施"，理由：当前票头本身用围栏，粗暴过滤会破坏现有协议）。
- 代码现状：`git log --oneline` 见 main `75b4b91`；全门 `bash bin/qwb-test.sh full` = **405 PASS / 0 FAIL**（44 秒）；fast 0.75s；无 worktree 残留；工作树干净。

## 四、本轮三个审点（只审这三点）

### 审点 1：你上轮的两条必须，处置是否成立
- ① 真实闭环：核对 `docs/E2E-RUNBOOK.md` 与 01 票验收记录——"跑通了但含人工介入、原始路径非无干预 PASS"的口径是否诚实、证据链是否支撑结论。
- ② 状态行解析未收敛：02 票的暂缓理由是否站得住（围栏协议冲突），还是应当立即做？给出你的判定和最小方案。

### 审点 2：三票新增机制的质量与边界
- 02 的 `--ensure`/`--check`/预检逻辑（`bin/qwb-wake.sh` watch_* 函数、`bin/qwb-run.sh` --pane 预检段）：抽查实现是否与验收声明一致；有无过度设计或新引入的脆弱点。
- 03 的 lint 第 7 节（`bin/qwb-lint.sh`）与 `tests/review-identity.sh`：结构校验的边界声明（不防伪造、不猜型号）是否与实现一致；有无假绿通道。
- `templates/TASK.md` 新增审核身份节、`qwb.config.sh` 接入：与既有场景冻结/疑点门是否冲突。

### 审点 3：现在这个产品，能否交付使用者日常使用
- 以使用者场景（只提需求、零通知、可见执行、跨会话续接、可审计、成本适当）做最终判定：**可以开始日常使用 / 还差什么**。
- 如果"还差什么"，列最小缺口清单（不超过 3 条），按你上轮的口径（产品够不够、流程是否过头）给一句话总评。

## 五、约束

- 除产出文件外只读；可在自己独有 /tmp 目录用真实 herdr 做小实验（用完清理，不动别的 workspace/pane）。
- 不重跑全门（主控已跑，405 PASS）；不重读全部 24 节 DECISIONS——按需抽查。
- **简洁**：先结论表后论证，总长控制在 800 字内；不凑数，没问题就说没问题。
- 最后一行打印 `FINAL AUDIT DONE <通过/不通过> <遗留缺口数>`。
