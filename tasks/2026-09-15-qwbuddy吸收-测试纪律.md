# 任务书：吸收测试与验收原则（快门全门 / 先场景后代码 / 自身 lint / 契约基线与假时钟）

```
任务 id:  qwbuddy-testing-discipline
state:    verified
来源:     使用者（Rocky）提出的六条测试/验收原则，主控评估后确定吸收其中 5 条（第 1 条未贴出）
派发:     主控（pi / qonnwolfbuddy 会话）→ 执行者（Devin CLI + SWE-2 High）
主账本:   /Users/rocky/projects/qonnwolfbuddy/tasks/2026-09-15-qwbuddy吸收-测试纪律.md
工作目录: /Users/rocky/projects/qonnwolfbuddy/.worktrees/qwbuddy-testing/
分支:     qwbuddy-testing
```

## 0. 背景与目标

QW buddy 是一个**装进项目的 AI 主控协作层**（不是应用软件）：一份说明书 + 几个 shell 脚本 + markdown 账本。它要解决的问题是「主控叫不醒、没有身份切换、没有派工标准」。

本票吸收五条外部测试原则，**关键要求：必须落成可执行的东西，不能只写进说明书正文**——否则违反原则本身（"能自动检查的规范不要只写在纸上"）。

五项吸收内容 **A / B / C / D** 全做，范围较大，请一次做完再报。

**范围（白名单）**：`bin/`（含新增脚本）、`templates/`、`tests/`（含新增 fixture 目录）、母本仓根新增 `qwb.config.sh`。**不要动 `docs/`**（文档同步由主控做）。

---

## A（必须）：快门 / 全门 —— `bin/qwb-test.sh`

**动机**：改两行代码不该等半天全套测试。

**规格**：

1. `templates/config.sh` 增加两条门声明（放在超时配置附近，带注释）：

```sh
QWB_GATE_FAST="bash -n bin/*.sh tests/smoke.sh && shellcheck bin/*.sh"
QWB_GATE_FULL="bash tests/smoke.sh && bash bin/qwb-lint.sh"
```

2. 新增 `bin/qwb-test.sh`：

```
用法: qwb-test.sh <fast|full> [--project <根>]
```

- 读配置：优先 `<项目>/qwbuddy/config.sh`，**若不存在则回退 `<项目>/qwb.config.sh`**（母本仓自用，它是模板源，不给自己装一套 `qwbuddy/`）。
- 用 `bash -c "$QWB_GATE_FAST"`（或 `_FULL`）执行，**工作目录 = 项目根**。
- **原样转发**命令输出（stdout + stderr 都要看得见），**退出码透传**。
- 门失败时额外打印一行，格式：`门失败（<fast|full>）：<命令原文> 退出码=<n>`，并写到 **stderr**。
- 未声明对应门时：报错并**打印这段声明的正确写法**（告诉项目该怎么加），退出码非 0。
- `--help`。
- **不得**用 `eval` 之外的方式拼命令；`set -euo pipefail` 下要把失败命令的退出码捕获后再自行决定退出。

3. 母本仓根新增 `qwb.config.sh`（**本仓自用**，声明自己的两条门）：

```sh
# 本仓（QW buddy 母本仓）自己的质量门声明
QWB_GATE_FAST="bash -n bin/*.sh tests/smoke.sh && shellcheck bin/*.sh"
QWB_GATE_FULL="bash tests/smoke.sh && bash bin/qwb-lint.sh"
```

---

## B（必须）：先场景后代码 —— 任务书模板 + 说明书规范

**动机**：不能让 AI 写完代码再照自己的实现证明自己没错。

**规格**：

1. 新增 `templates/TASK.md`——**任务书模板**，结构：

```
# 任务书：<主题>
<头部字段：任务 id / state / 来源 / 派发 / 主账本（绝对路径）/ 工作目录 / 分支>

## 0. 背景与范围

## 1. 验收场景（先写场景，再写代码；场景冻结后才许可提交实现）
### user_<正常路径场景名>
Given <前置状态>
When  <动作>
Then  <可见结果>
### user_<失败路径场景名>      ← 失败路径场景**至少一条**
Given …
When  …
Then  …
（场景命名：项目有测试框架时以 `user_` 开头；没有测试框架时用同名小节标题即可）

## 2. 硬约束
## 3. 验收门（快门 / 全门分别是什么）
## 4. 报告要求（状态行、DONE/STOP）
## 5. 本票不允许做的事
```

2. `templates/QWBUDDY.md` 新增一节（放在「验货门」附近），写清：

- **先场景后代码**：派发前任务书**必须**有「验收场景」块；场景用 Given/When/Then；**至少一条失败路径场景**（只写 happy path 的任务书不完整）。
- **场景冻结**：场景定稿后才许可提交实现；**实现完成后不得回头改写场景以迎合实现**——那是自证。
- **测试分级**：项目要声明**快门**（快、无外部依赖，改一行跑它）与**全门**（完整）；派活/自检跑快门，**合并前跑全门**；用 `qwb-test.sh fast|full`。

3. `templates/roles/执行者.md` 的「禁止事项」增加两条：

- 在实现完成之后**改写/补写验收场景**以迎合实现（自证）
- 用**没有契约校验的假替身**冒充真实系统

---

## C（必须）：QW buddy 自己的 lint —— `bin/qwb-lint.sh`

**动机**：能自动检查的规范不要只写在说明书里；且我们已被"写在纸上的规范"坑过两次（当初 DESIGN 承诺的「锁」半年无人发现没实现；worktree 收尾规范写在说明书里而没有任何实现）。

**规格**：新增 `bin/qwb-lint.sh`，`--project <根>`（默认当前目录），逐项检查并输出 `PASS/FAIL`，最后汇总，**任一 FAIL → 退出码 1**。

检查项：

1. **文档承诺的脚本必须存在**：从 `<项目>/qwbuddy/QWBUDDY.md`（以及存在时的 `docs/DESIGN.md`）里提取所有形如 `qwb-*.sh` 的名字，每一个都必须在 `<项目>/qwbuddy/bin/`（母本仓则是 `bin/`）存在。缺失即 FAIL 并列出名字。
2. **账本 state 合法**：`<项目>/tasks/*.md` 里出现的 `state:` 值必须 ∈ `running|blocked|needs-decision|done|verified`；非法值 FAIL 并指出文件名与值。
3. **config 无死键**：配置里每个 `QWB_*` 变量都必须在某个 `bin/*.sh` 里被引用；出现没被引用的键即 FAIL（防"配置写了没人读"）。
4. **无「变量后紧跟非 ASCII」写法**：扫描 `bin/*.sh`，出现 `$VAR` 后紧跟非 ASCII 字符（应写成 `${VAR}`）即 FAIL——这条把错题本里的坑固化成 lint。

要求：纯 shell（可用 `grep`/`sed`/`awk`/`perl`，**不引新依赖**），**不要**在运行时脚本里用 `python3`。

---

## D（必须）：假替身要有契约基线 + 可注入时钟

**动机**：① 现在的 herdr stub 是硬编码 `echo`，**没有校验它和真实 herdr 的契约**——herdr 换字段名，测试照样绿；② 现在测等待预算只能真 `sleep`，属于"睡几秒再祈祷"。

**规格**：

1. **契约基线**：新增 `tests/fixtures/herdr/`，把**真实 herdr 的响应**存成样本文件（每条命令一个文件），至少覆盖脚本实际依赖的调用：`tab create`、`agent start`、`agent prompt`、`agent wait`、`agent list`、`pane run`。
   - 样本必须是**真实**输出（用 `herdr <cmd>` 在真实会话里录；录不到的场景要在文件头注释说明是构造的**并按真实输出形状**写）。
   - `tests/smoke.sh` 里的 herdr stub 改为**从 fixture 读**（`cat "$FIXTURE"`），不再硬编码 JSON。
2. **契约断言**：smoke 增加一条检查——fixture 里必须存在脚本所依赖的 JSON 字段路径，至少：`.result.root_pane.pane_id`（`tab create`）、`.result.ok` 或存在性（其余）。字段缺失即 FAIL 并指出。
3. **可注入时钟**：`bin/qwb-wake.sh` 的计时与睡眠改为可注入：
   - `now_ms()`：若 `QWB_NOW_MS_CMD` 非空则执行它取毫秒值，否则用现有 `perl -MTime::HiRes` 实现。
   - `sleep_ms()`：若 `QWB_SLEEP_CMD` 非空则执行 `"$QWB_SLEEP_CMD" "$ms"`（把毫秒传给它），否则用现有小数秒 `sleep`。
   - 两个变量**只用于测试注入**，默认行为必须与现在**完全一致**（不得改变默认等待语义）。
4. **smoke 用假时钟测预算**：新增断言——用一个"每次调用递增 N 毫秒"的假时钟 + 立即返回的假 sleep，验证「一轮 = 1×interval」的预算逻辑（**不再真 sleep**）。同时保留一条真时钟的轻量冒烟（真跑一轮即可，不要跑 4 秒）。

---

## 硬约束（不变）

零通知使用者；无队列/ACK/数据库/守护进程/cron；只用 Herdr；超时一律毫秒；工人禁 headless；**纯 shell 零外部依赖**（`grep/sed/awk/perl/sleep` 可用；**`python3` 只允许出现在 `tests/smoke.sh`**）；变量后紧跟非 ASCII 字符一律 `${VAR}`；**不要用字符串拼接去绕任何断言**（那等于让门失效）。

## 验收门（你必须先自己跑通，主控会独立重跑）

1. 快门：`bash bin/qwb-test.sh fast`（在本仓根跑）→ 退出码 0。
2. 全门：`bash bin/qwb-test.sh full` → 退出码 0，且输出里能看到 lint 的逐项 PASS。
3. `bash tests/smoke.sh` 全过（现有 126 项不许退化），并新增 A/B/C/D 的断言（预计新增 10 项以上）。
4. **假时钟生效证明**：跑一次"全门"，把总耗时与改动前对比贴出来（预期**变快或持平**，不得因为新增断言而显著变慢）。
5. `grep -rn 'python3' bin/` 必须**零命中**（运行时脚本不许用 python3）。

## 报告要求

往主账本绝对路径（本文件）追加状态行 `working:` / `done:`（含跑了什么命令与原始结果）/ `blocked:`。`done:` 里要写明：新增/修改了哪些文件、每种 fixture 是真录还是按形状构造、假时钟注入的实际机制。最后打印 `DONE <commit sha>` 或 `STOP <原因>`。**不要改本文件的 `state:` 字段。**
done: A/B/C/D 全部落地，验收门全绿（worktree qwbuddy-testing，commit 836df56）。
  新增：bin/qwb-test.sh（快门/全门执行器，qwbuddy/config.sh→qwb.config.sh 回退，bash -c 原样转发+退出码透传+门失败行）、
        bin/qwb-lint.sh（四查：文档承诺脚本存在/state 值域/config 死键/$VAR+非ASCII，纯 shell 无 python3）、
        qwb.config.sh（母本仓自用两条门声明）、templates/TASK.md（任务书模板，含 Given/When/Then 与≥1 失败路径场景）、
        tests/fixtures/herdr/ 8 个契约样本。
  修改：bin/qwb-init.sh（装 TASK.md）、bin/qwb-wake.sh（now_ms/sleep_ms 支持 QWB_NOW_MS_CMD/QWB_SLEEP_CMD 注入，默认行为不变）、
        templates/config.sh（QWB_GATE_FAST/FULL 声明）、templates/QWBUDDY.md（新增 §6 测试纪律，原 §6-10 顺延 §7-11，§9 表补两脚本行）、
        templates/roles/执行者.md（禁止事项+2：改场景自证、无契约校验假替身）、tests/smoke.sh（stub 改读 fixture、§12/12b/12c 改假时钟确定性断言、新增 §12d/12e/23/24/25/26）。
  fixture 来源：全部真录（2026-09-15 herdr 0.8.2 真实会话：tab create→w93:t7/pane w93:p7 真建真关、pi agent 真起真提示真等待、
        agent list 真清单、wait 超时与 pane_not_found 真错误输出；pane run 成功契约=stdout 空+退出码 0，fixture 仅注释行）。
  假时钟机制：QWB_NOW_MS_CMD 指向每调用 +STEP 的脚本取毫秒；QWB_SLEEP_CMD 指向只记参数不真睡、满 N 次 kill 父进程截断循环的脚本；
        stub 的 agent wait 支持 HERDR_WAIT_BUMP_MS 把假时钟前推以模拟等待耗时；默认（两变量为空）行为与改动前完全一致。
  证据：bash bin/qwb-test.sh fast → 0；bash bin/qwb-test.sh full → 0（smoke 184 PASS/0 FAIL + lint 4 项 PASS）；
        grep -rn 'python3' bin/ 零命中；断言 126→184 无退化。
  耗时：改动前 smoke（≈全门最重部分）15.9s → 改动后全门 8.8s（去掉 3×4s 真 sleep，加假时钟断言后反而更快）。

---

## 验收记录（主控 / pi 会话，2026-09-15）

提交 `836df56`（worktree 分支 qwbuddy-testing）。

**质量门（主控独立重跑）**

| 门 | 结果 |
|---|---|
| `bash -n` ×8 | 全通过 |
| `shellcheck bin/*.sh` | 退出码 0 |
| `grep -rn python3 bin/` | **零命中** |
| `qwb-test.sh fast` | 退出码 0 |
| `qwb-test.sh full` | 退出码 0，**188 PASS / 0 FAIL**，耗时 **9 秒** |
| `bash tests/smoke.sh` | 184 PASS，退出码 0 |
| 四套主控自建门（首轮 28 / F1–F4 16 / G1–G4 18 / R4 11） | **73 项全 PASS**，无回归 |

**A 快门/全门** ✓ —— `fast` 与 `full` 都跑通；母本仓用根目录 `qwb.config.sh` 声明自己的门。

**B 先场景后代码** ✓ —— `templates/TASK.md` 模板含「验收场景」块（Given/When/Then + `user_` 命名 + **失败路径场景至少一条**）；`QWBUDDY.md` §6「测试纪律」三条（先场景后代码 / 场景冻结 / 测试分级）；`roles/执行者.md` 新增两条禁止（实现后改写场景自证、用无契约校验的假替身）。

**C 自身 lint —— 主控刻意验证它不是「永远 PASS 的假 lint」**：

| 造出的违规 | lint 结果 |
|---|---|
| 文档提到不存在的脚本 `qwb-nonexistent.sh` | **rc=1**，输出 `FAIL 文档点名但 …/bin 缺失: qwb-nonexistent.sh` |
| 账本 `state: pending` | **rc=1** |
| 配置死键 `QWB_DEAD_KEY` | **rc=1**，输出 `FAIL 死配置（bin/ 无引用）: QWB_DEAD_KEY` |
| `bin/` 里写入 `echo "值=$QWB_WORKERS（测试）"` | **rc=1** |
| 干净项目 | **rc=0**（不误报） |

**D 契约基线与假时钟** ✓ —— `tests/fixtures/herdr/` **8 个真录样本**（文件头写明 `真录 2026-09-15，herdr 0.8.2 … 真实会话 w93:t7，录完即关闭`）；stub 改为从 fixture 读；`qwb-wake.sh` 新增 `QWB_NOW_MS_CMD` / `QWB_SLEEP_CMD` 注入点（默认行为不变）。

**耗时对比（假时钟的直接收益）**

```
main      smoke.sh  126 项 → 16 秒
本票后    smoke.sh  188 项 →  9 秒   （断言更多、反而更快）
```

**结论**：验收通过，`state: verified`。文档同步（DESIGN/README）由主控完成；随后按使用者要求交 **Astra high 整体二审**。
