# 场景完善 03：审核身份可核验

state:    running
任务 id: scenario-03-review-identity
依赖: 01 基线已完成；经主控拆分，本票不依赖02代码，可以并行实现，合并后再集成验收
执行者: zcodecli（用户要求只保留一个Devin；本票转交ZCode）
交接: 前任Devin quiver-amaranthus / w93:p0 已按用户指示停止；停前工作树干净，无需重做或丢弃改动。
受理状态: ZCode调用未收到receipt，list未找到本票，暂不宣称已执行。当前本workspace窗口w93:p12/tab w93:t11；幂等键qwb-scenario03-identity-zcode-1（各次使用同键，不重复执行）。先前独立chat窗口w93:p11无受理记录已回收。
阻塞边界: 不修改zcodecli/Herdr源码与全局配置；工具问题归用户指定的主项目处理。worktree .worktrees/scenario-03-identity 保留供工具恢复后接手，主控负责最终收尾。
冻结白名单: bin/qwb-lint.sh、templates/TASK.md、tests/review-identity.sh（新增定向测试）、qwb.config.sh（仅接入该定向测试）。不改 bin/qwb-run.sh、qwb-wake.sh、qwb-status.sh、templates/QWBUDDY.md、tests/smoke.sh，避免与02抢文件。

## 目标
把已要求的独立审核变成可检查的证据，不能把“不同 CLI/provider”误认成“不同模型家族”。不建动态模型注册表，不增加普通票的无用必填字段。

## 验收场景

### user_换CLI不等于独立审核
Given 实现者是 Codex 上的 GPT 系模型，审核者是 Pi 上的同家族模型
When 对已要求独立审核的票检查身份
Then 不得凭 CLI 不同报通过；报错明确指出同家族

### user_不同模型家族审核有效
Given 真实会话记录分别确认实现者和审核者的 CLI、模型与家族，且审核者不是同一会话换角色
When 核对票内证据
Then 接受其独立性元数据；不因此宣称代码审核结论必然正确

### user_身份未知不伪装通过
Given 会话缺模型信息或模型家族无法可靠判断
When 核对一个已要求独立审核的交付
Then 输出身份未确认，需要主控补证据；不凭名称猜测、不能标独立审核已完成

### user_无审核要求的普通票不额外烧token
Given 普通小票本身未要求独立审核
When 检查票格式
Then 不强制启动审核 agent、不新增模型调用，只跳过相关检查并明确范围

## 最小实现约定（不做第二个派发器）

- 只在已要求独立审核的票启用（可用 review-required: yes 显式标记）。没有该标记的历史/普通票不强行补字段、不引额外模型调用。
- 在TASK模板加一段可选审核身份记录：实现者/审核者各自的实际model、family、原生session与证据位置；CLI/provider可附记，但绝不充当family。身份由主控据真实TUI/会话证据填写，本票不访问模型服务、不新增注册表或自动猜型号。
- lint检查适用票的字段完整性、非unknown family、两方family不同、原生session不是同一实例。无法确认则报缺证据，不把unknown与另一个字符串不等当通过。只按显式声明与证据引用做结构校验，不能宣称防恶意伪造或证明模型实际使用。
- 允许用独立测试文件承载全部user Given/When/Then正反例；qwb.config.sh全门接入该文件。不得搬运/重排原smoke文件，不改02正在改的运行时文件。
- 反转一次family相等的拒绝条件，保证新的定向测试变红，然后恢复；不能仅检查文案。

## 实施原则
先看现有任务/会话记录的最小元数据，不重读全模型目录。CLI 与模型家族分开记录；无法自动给出 family 时，由主控显式声明并附原生 session 证据。只在需要审核的票记录，已有历史票不得凭空补身份。
不提供全模型映射数据库；本轮审核过程仍最多三个审点，用用户指定或全局规定的审核模型，不强制修改 provider。

## 验证
先写上面的 user Given/When/Then 正反例；同 CLI 不同模型、不同 CLI 同家族、未知身份分别覆盖。若声明自动校验则对实际入口做反转验证，不只测文案关键词。
合并前主控独立 fast/full 与新增定向测试；与02一起做一次有限审核，确认映射与证据边界。不要另开研究/审核子代理，不commit/merge/push。完成只追加本票进展，报告实际命令/退出码、反转验证结果与限制。

## 派发记录（最终轮）
- zcodecli 接收管道两次提交无 receipt（使用者正在修 zcodecli 主项目）；使用者指定改派 Devin。
- 旧 worktree 基于 eb50d13（02 票合并前），工作树干净已回收，从 main 重建同名 worktree，基线含 02 票全部改动。
- 白名单不变：bin/qwb-lint.sh、templates/TASK.md、tests/review-identity.sh（新增）、qwb.config.sh（仅接入定向测试）。qwb-wake/run/status/QWBUDDY/smoke 以 main 为准不动。
