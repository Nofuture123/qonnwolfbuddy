# 错题本索引

一行一条，详情见 `lessons/<主题>.md`。每次踩坑先在这里加一行，再写详情文件。

| 日期 | 一句话 | 详情 |
|---|---|---|
| 2026-09-15 | shell 脚本里 `$VAR` 后紧跟非 ASCII 字符（全角括号/逗号）会被 bash 当成变量名的一部分，`set -u` 下直接 `unbound variable` 崩溃 | [shell-变量后紧跟非ascii字符](lessons/shell-变量后紧跟非ascii字符.md) |
| 2026-09-15 | `set -e` 下函数末行用 `[[ cond ]] && echo` 收尾，cond 为假时函数返回 1，调用点会**静默打断整个循环** | [set-e-下函数末尾短路返回值](lessons/set-e-下函数末尾短路返回值.md) |
| 2026-09-15 | 去重/变更检测不能挂在「只有单方能改」的字段上——把键挂到不会变的那一侧，等于把信号永久吞掉 | [去重键不能挂在单方能改的字段上](lessons/去重键不能挂在单方能改的字段上.md) |
- [弱门会被优化：用字符串拼接「通过」自己的验收门](lessons/弱门会被优化成字符串拼接.md) —— 2026-09-15
- [按场景验收而非按机制数量验收](lessons/按场景验收而非按机制数量验收.md) —— Rocky 2026-09-15 纠正：先判断场景满足度，区分设计与实证，不越界修改其他工具。
- [按改动边界并行](lessons/按改动边界并行.md) —— Rocky 2026-09-15 纠正：独立模块不必等单个执行者串行推进。
- [临时项目谁建谁清](lessons/临时项目谁建谁清.md) —— Fable 终审抓的 qwb-live 残留：验收人建的真实试验项目也要当场删，不留给"下一票复验"。
- [执行者的产出由执行者窗口收尾](lessons/执行者产出由执行者窗口收尾.md) —— Rocky 2026-09-15 纠正：agy 重写 README 后，主控自干了三处事实修正；执行者产物的修订也要退回原执行者窗口，不因「小」而自干。
- [手工算场景指纹要用 lint 同款算法](lessons/手工算场景指纹要用lint同款算法.md) —— 主控 2026-09-16：母本仓手工派发时 `awk|shasum` 多算块尾换行，三票被 lint 误判改场景；照抄 `$(…)` 去尾。
- [主控窗口与当日模型路由](lessons/主控窗口与当日模型路由.md) —— Rocky 2026-09-22：主控窗口不分 pane；今日执行 GPT Sol medium、审核 GPT Sol high。
- [证据时间必须取自机器](lessons/证据时间必须取自机器.md) —— 2026-09-22：账本不写估计时间；错误记录保留并追加实测更正。
- [审核结论先说人话](lessons/审核结论先说人话.md) —— Rocky 2026-09-24：技术审核结论对话里先给大白话（结论一句、每个问题讲后果和建议），证据细节放报告/任务书。
- [定级与放行前跑通真实链路](lessons/定级与放行前跑通真实链路.md) —— 主控 2026-09-24：三次先下结论、后被实测推翻（没对照计划条款就说可合并，严重度两次改口，推演重名时用错输入）。定级前实测到使用者能看到的终点，推演前先读产生输入的代码。
