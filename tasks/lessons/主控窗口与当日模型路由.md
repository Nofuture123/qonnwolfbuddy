# 主控窗口与当日模型路由

2026-09-22，Rocky 明确纠正：本窗口禁止开 pane，已开的执行/审核 pane 要调走。主控 tab 保持单 pane；后续复用已移到后台 tab 的窗口，不再分割主控窗口。

今日执行指定 GPT Sol medium，审核指定 GPT Sol high。用户明确路由优先于模板默认或跨家族建议；执行和审核仍使用独立原生会话。换模先停止旧会话、保留工作区，再核对新 TUI 的实际模型/effort 后投递任务。不把未完成的旧会话工作当验收证据。

同日补充：用户点名窗口时，先按准确 label 在全部 Herdr tab 中定位，不能只查当前 workspace 后用相似窗口代替。qwb-常驻附页是旧功能开发的执行会话，不是 qwb-值守；判断能否关闭须核实进程、任务收尾及 worktree 状态。
