# 按改动边界并行，避免不必要串行

来源：Rocky 2026-09-15 指出单个 Devin 太慢，要求可多线程推进。

- 先识别真实依赖，而不是因为都属于同一项目就串行。
- 两项任务修改不同模块时，各开独立 worktree 与可见 tab，分别限定文件范围。
- 共享测试入口可拆为新增定向测试，由一方维护汇总入口；避免两个工人同时重写 smoke.sh。
- 进行中的工人不为凑并行而中断重做；把独立的新工作交给另一工人。
- 并行后仍由主控独立验收并整体验证兼容性，不把独立通过当成合并后一定通过。
- 不设置常驻空岗位，不为无依赖关系的探查再开一轮长篇研究。
