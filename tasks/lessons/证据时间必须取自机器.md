# 证据时间必须取自机器

2026-09-22 生产收敛中，执行者两次在账本写入估计的未来整点时间。证据时间必须由 `date -u`、工具时钟或 Git 原始提交时间取得，不能根据日期和上下文猜测。

发现已追加记录时间错误时，保留原记录，只追加带真实机器时间的更正行；提交完成时间可用 `git show --format=%cI <SHA>` 复核。测试退出码、SHA 与清理结果也各自引用实际结果，不能随时间纠错一并补造。
