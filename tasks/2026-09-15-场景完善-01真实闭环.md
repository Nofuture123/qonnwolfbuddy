# 场景完善 01：真实闭环基线

state: verified
任务 id: scenario-01-live
依赖: 无
后续: 02 值守生命周期；03 审核身份检查
结项口径: 已完成基线调查和证据核对，不是原始默认无干预路径通过。报告见 docs/E2E-RUNBOOK.md。
主控核对: 临时项目 main 的 0a3a155、PROJECT_NOTE.md 唯一 token、任务结果行及 worktree merged 记录一致；worktree list 只剩主目录，工人/值守 tab 已回收。现场观察后续验收回合中的负例门=1、恢复门=0。当前源代码 fast/full 独立运行 exit=0（328条PASS）。
资源记录: 验证主控 sunny-temple，工人 mixolydian-yamamomo；临时项目 qwb-live.ZCBo9DXOVf 保留到02复验，主控负责最终清理，不得当成长期工程资产。
基线: e917008（运行时与 e283913 相同；Fable 意见是待验证建议，不是已证实缺陷）
执行者: Devin SWE-2 Max，可见 Herdr 独立 tab
产出: docs/E2E-RUNBOOK.md（唯一报告，不另建长篇审核账本）

## 目标与范围

Rocky 只提需求，不应替主控启动值守、追问进度或手动转发完成通知。本票先验证现有代码，不增加新机制。观察真实安装→隔离派发→工人结果→值守叫醒存活主控→独立验收→收尾。

测试宿主限定：用 mktemp 创建专用 qwb-live 临时 Git 项目；不得碰其他项目或已存在的 worktree。源代码只读：bin/、templates/、qwb.config.sh、tests/smoke.sh；写入仅限本报告、本票追加进展、专用临时项目。不得修改本仓 runtime、全局配置、zcodecli、Herdr 安装文件；不得 commit/merge/push 本仓。

## 验收场景（先确定再执行）

### user_只提需求后主控被真实叫回
Given 专用临时项目已安装 QW buddy、声明可真实运行的项目门；专用主控与工人各在独立可见 tab，主控存活且已空闲，值守存活
When 工人完成一项真实文件任务并向主账本追加结果，qwb-wake.sh 实际向主控发送提示
Then 主控必须产生新的原生会话回合，主动读取账本、独立检查文件与退出码、记录验收；不能以命令返回 0、出现 wake 行或终端回显代替证据；Rocky 和主控代理不得手工转发“工人已完成”来伪造闭环

### user_主控忙时不丢工人结果
Given 专用主控正在执行有意义的真实任务，工人有新结果
When 实际值守投递
Then 记录该 CLI 的排队/打断/拒绝行为与后续回合；区分“收到提示”和“已验收”。若丢提示，保留复现与账本而不现场加机制；无法复现忙态也须诚实标未验证，禁止制造整段 sleep 的忙态

### user_验收失败不被标成完成
Given 临时项目门检查真实产物，独立验证者先使产物不符合预定场景
When 主控验收
Then 项目门非 0，不把业务任务结项为 verified；恢复产物后才允许通过和 worktree 收尾

### user_收尾只清自己的临时资源
Given 本轮 session、pane、worktree 与报告结论均已记录
When 本轮验证结束
Then 临时子 pane 用完即关；停止写入后按 git 实际状态合并或 archive，再 remove/prune；不凭“无未结票”自动认定别人的 worktree 可删

## 执行方式与成本约束

- 复用当前 Herdr workspace，但主控 tab 不分屏；本票最多一个验证主控（你自己）+ 一个真实工人 + 一个纯 shell 值守 tab；不派研究/审核子代理。
- 只用用户指定 Devin SWE-2 Max，关闭 fast/priority。工人由 qwb-run.sh 真实派发；可先用 Herdr tab 的环境参数指定 DEVIN_MODEL=swe-2-max 与许可模式，必须核对 TUI 实际模型，不把裸 devin 默认模型当已确认。如果命令无法传达模型约束，记录阻塞，不能用假 herdr/wrapper 返回成功。
- worker 只负责临时项目一项小文件交付（如创建和校验项目说明），不扩功能；会话及成功标记使用运行时唯一值，避免提示回显假阳性。
- 不导出 shell 函数覆盖 herdr/git/date/sleep；不得用固定 fixture 假装实时契约。真实集成试验允许真实耗时，预算逻辑单测才用 fake timer。
- 先做一次最小正常路径，再按需补失败/忙态；同一阻塞最多两次尝试，约 30 分钟内给出可追踪进展，不无限烧 token。
- 记录实际 CLI/provider/model、session、pane、源版本、任务/产物路径、真实命令与退出码、人工介入点、时间线。报告明确 PASS/FAIL/未验证；前台脚本不等于“一定跨天死亡”，不能用推测当实测。
- 若发现与票有矛盾，追加 blocked: spec-defect: 条款/反例/影响；不擅自绕过，主控裁决。

## 质量与交付

本仓只读基线门：bash bin/qwb-test.sh fast；必要时 full（各记录真实退出码）。不改源代码以让试验变绿。
报告最后列：已经满足的场景、确定阻塞、需要 02/03 修复的最小清单，避免转化成全仓重审。
若主控需要空闲等唤醒，在报告/本票先记录 WAITING 与相关 pane 后结束当前回合；流程允许由值守唤醒原会话续接。

## 执行进展（scenario-01-live，验证主控 w93:pH）

working: 2026-09-15T18:39+02:00 临时项目 /var/folders/.../qwb-live.ZCBo9DXOVf 实装实派：worker=devin(SWE-2 Max 经 TUI 确认) pane=w93:pJ tab=w93:tH session=mixolydian-yamamomo；值守 shell pane=w93:pK tab=w93:tJ interval=20s 已第一轮叫醒主控。现象A：qwb-run 退出0但首次 agent prompt 被 trust 对话框吞掉，工人停 idle，经人工 Enter+补发恢复（记为人工介入，原始路径非无干预 PASS）。worker --pane 复用时 cwd 不随 worktree（实测 pane cwd=项目根）。工人已 commit 0a3a155 并自检门 0。报告 docs/E2E-RUNBOOK.md state=WAITING，本回合释放待值守二次唤醒后独立验收。
done: 2026-09-15T18:44+02:00 最小闭环实测完成：worker done:→值守 fp 变→wake#2→主控新原生回合独立验收（内容/commit/门 fast=0 full=0）→篡改测试（门 fast=1 full=1 拦截）→恢复→verified→merge+finish --merged（exit 0，分支已删）→停值守→回收 w93:tH/tJ。现象A（prompt 被 trust 吞、qwb-run 返回0但工人未开始）与 --pane 不随 worktree 改 cwd 已记录为待处置项；2 处人工介入已登记，原始路径非无干预 PASS。忙态丢提示未构造真实条件，标未验证。全部证据 docs/E2E-RUNBOOK.md。
