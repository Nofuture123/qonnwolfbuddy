# 场景完善 02：使用者不再手工启动值守

state:    verified
任务 id: scenario-02-watch
依赖: 01 真实闭环完成或明确产出确定阻塞；主控读取其结论后再派发
执行者: Devin SWE-2 Max
实施基线: 01 报告已出，证实值守能叫回独立会话，但首次 trust 提示会阻断派发，--pane cwd 也可能与 worktree 不一致。01 原始默认路径未判无干预通过。
冻结白名单: bin/qwb-wake.sh、bin/qwb-status.sh、bin/qwb-run.sh、templates/QWBUDDY.md、templates/config.sh、tests/smoke.sh、tests/fixtures/herdr/（仅本票健康/就绪查询的真实响应基线）；仅确有重复的健康查询代码时可加一个 bin/lib/ 辅助文件（须同时验证安装复制）。docs/ 报告由主控更新。

## 目标
主控开局自己启动或复用值守；使用者不需要当启动员。值守失活可见，但不承诺主控退出或机器重启后自动复活。保持纯 shell、Herdr、账本的现有形态。

## 验收场景

### user_开局无需手工启动值守
Given 项目已安装、主控存活、尚无本项目有效值守
When 主控按开局步骤操作
Then 当前 workspace 内创建一个可见的值守 tab，记录身份，启动 qwb-wake.sh；重复开局复用而不重复创建或抢占其他项目 pane

### user_失活值守明确可见
Given 账本记录的值守已退出或 pane 仍在但其值守进程不在
When status/开局检查
Then 明确显示未运行或未知，不把“pane 存在”当健康；主控存活时可重新启动，不通知使用者、不启守护进程

### user_忙态不丢结果
Given 01 已确定当前 CLI 在主控忙时的真实投递行为
When 修订投递处理（仅在 01 证明有缺陷时）
Then 依据真实行为做最小修复，结果仍留账本；不因文字写入成功就虚报已被主控处理

### user_日志引用不成为控制指令
Given 01/定向复现证明代码围栏内的 done/spec-resolved 等文本被误认作状态
When 解析同一任务书
Then 明确区分正式状态记录与引用文本。先核对历史票把 state 写在代码围栏里的约定，不得统一“忽略所有围栏”而让现有票全部消失；有兼容方案和真实回归才收敛到共用解析入口

## 本轮落地要求（基于01，不扩新架构）

1. 提供幂等的值守确保入口（优先 qwb-wake.sh --ensure，复用原命令，不新建服务）：主控开局调用，显式目标 pane/项目；只能在调用者自身 workspace 开/复用可见 shell tab。重复调用最多一个有效实例，不杀别的 pane。识别依据须含实际项目与值守进程，不凭 pane 存在或名字相似就报存活。未知身份/无法查询时明确报错，不擅自重开多个实例。
2. status 显示本项目值守“运行/未运行/未知”，开局说明让主控先确保值守，再派活；不承诺主控已死时还能自动修复。
3. --pane 复用要在派发副作用前核对 cwd 与目标 worktree，无法确认或不一致则拒绝并给明确修复步骤；不要往未知 TUI 发 cd。首次目录 trust 是一次性项目授权/准备步骤，不自动接受任意未信任项目、不关闭全局信任规则。文档与返回信息明确“已投递≠工人已开始”，不得靠隐式补发掩盖失败。
4. 先写 user Given/When/Then 正反例，fake Herdr 只用于单测且需契约校验；主控最终用真实 Herdr 重跑确保/重复/失活检测与准备好信任后的默认闭环。没有依据的忙态投递重试、自动确认、自动重启主控全部不做。
5. 围栏/共用解析器只登记可复现证据，不在本次实施；原因：当前票头使用围栏，粗暴过滤会破坏现有协议，需单独设计后再动。

## 实现边界
优先复用现有脚本和 Herdr 查询，不凭空加 supervisor、注册中心、轮询服务、通知通道。启动身份记录只保留最低所需字段，不能把其他项目的空闲 pane 当本票所有。
围栏问题须先复现后定范围，是协议一致性修复，不做通用 Markdown 解析器。预算测试用 fake timer；真实跨 CLI 行为使用真实集成试验。

## 交付与验证
先根据 01 证据形成短规格，再写代码；新增 user 开头 Given/When/Then 场景测试。fast/full 由主控独立运行；一次真实冷启动→重复开局→值守退出→主控恢复试验，证据追加同一 E2E-RUNBOOK。
不删场景冻结、不动历史报告、不修改 Pi/Herdr/zcodecli 源码。验收与限定复审通过后才允许合并。
working: 2026-09-15T17:04:55Z 执行者开工：完成真实侦察（herdr pane get/process-info/pane list/tab create/agent wait/prompt 形状实录）；tab create 不带 --workspace 会落到 UI 焦点 workspace 而非调用者所在——ensure 必须显式传 --workspace；空 shell 判定=foreground_process_group_id==shell_pid；值守进程可识别=前台组内含 bash qwb-wake.sh 本体+子进程；devin 首次 trust 框实录（Do you trust the authors…/Yes, trust）其时 agent_status=idle（证实 01 现象A 吞提示词）；已录真实 fixture 素材。

working: 2026-09-15 scenario-02-watch 执行者完成实现（收窄后三件事）。
- --ensure 幂等确保：qwb-wake.sh 新增 --ensure/--check；健康判定=herdr pane process-info 前台进程组里真实 qwb-wake.sh 进程且 --project/进程cwd 与本项目 realpath 一致，不凭 pane 存在；登记在 qwbuddy/.watch；全量扫描 pane list 非 agent pane；复用/原地重启(idle)/另开tab(busy|gone)/双实例报错零副作用/查询失败拒动；并发用独立 qwbuddy/.watch.lock（不用主控锁，主控长期持有）。
- status 值守段：qwb-status.sh 调 qwb-wake.sh --check，输出「值守：运行/未运行/未知」；无 herdr、pane list 失败、登记 pane 查询失败均报未知。
- --pane 预检（qwb-run.sh）：账本写入与 agent start 之前核对 pane get 存在、无 agent 字段、process-info 前台组==shell_pid（空闲 shell）、pane cwd realpath==目标目录 realpath；不符拒绝并给 cd 修复/换 pane/新开 tab 指引。
- 模板 templates/QWBUDDY.md §1 开局第 7 步 --ensure、§9 脚本表、§11 边界同步。
- 收窄执行：未做通用 trust 对话框识别/自动确认/补发；01 的 trust 现象仍是一次性人工授权边界。
测试位置：tests/smoke.sh 第42节（ensure 幂等/复用/重启/占用/消失/跨workspace/双实例/无上下文/推导/查询失败/互斥）、43节（status 运行/未运行/未知全态）、44节（--pane cwd+状态预检正负例）；新 fixture tests/fixtures/herdr/{pane-get-shell,pane-get-error,pane-list,proc-shell,proc-wake,proc-busy,pane-read-shell.txt,pane-read-trust.txt}（真录 herdr 0.8.2）；stub 支持 HERDR_DYN_DIR 逐 pane 片场。
门结果：bash bin/qwb-test.sh fast → 退出码 0；bash bin/qwb-test.sh full → 退出码 0（tests/smoke.sh SMOKE PASS 368 项 + qwb-lint.sh LINT PASS，含一条既有旧制派发警告非本次引入）。
临时资源：探测 tab w8Z:tR/pY 已于侦察后 herdr tab close 回收；本轮实现阶段未开新 herdr 窗口、未派 agent；/tmp/dbgens 调试目录已删。无 commit/merge/push。

## 主控独立验收：第1轮不通过（未合并）

源基线 eb50d13，执行者未提交；主控独立 fast=0（0.634s），full=0（28.579s；368 smoke + 6 lint，共374 PASS）。源码差异为 bin/run、status、wake、QWBUDDY、smoke，加8份fixtures。门通过不代表以下场景通过。

真实 Herdr + mktemp Git 项目反例（未覆盖 herdr/git 函数、未使用fixture模拟；本轮试验pane与临时项目已回收）：
1. ensure 指向A(w93:pV)创建watch(w93:pX)；同A再次ensure复用通过；改传B(w93:pW)后ensure仍rc=0并称复用，实际process-info argv仍含 --pane w93:pV。违反换主控后确保正确值守的语义。
2. 将watch Ctrl-C后，shell仍在，--check正确报“未运行”，此项通过。
3. --pane cwd=项目根，目标默认worktree=.worktrees/cwd：run最终rc=1，但实际先输出 Preparing worktree/new branch 并创建该目录，之后才检查cwd。任务书未改，但派发前零副作用要求未满足。
4. 项目路径含单引号（/tmp/qwb-s02-quote'-...）：ensure rc=0“已建tab并投递，但尚未确认进程出现”；紧接--check报未运行。WATCH_CMD直接用单引号包路径而未转义，启动失败被当成确保成功。

## 返修要求（同线程，限三组）

### user_切换主控不能复用错误目标
Given 本项目已运行指向A的值守
When ensure显式要求B
Then 不能成功静默复用A；须核实项目、真实值守命令、目标pane及workspace。可安全地只处理本项目实例，或非0拒绝并给主控明确操作步骤；绝不能擅自杀其他pane。查询失败/候选身份无法确认时不得忽略WATCH_QERR并新开实例。--ensure/--check/--once等短调用本身不算持续值守实例。

### user_cwd预检失败不制造资源
Given --pane cwd与预计目标目录不符（包括--revise-scenarios与默认创建路径）
When run
Then 拒绝发生在创建worktree/分支、改场景基线/写任务书和启动/投递之前；任务原字节不变、不新增worktree/tab。先推导预期目录再预检，目录尚未建立也不能因此先创建再判断。

### user_启动没有确认就不能报确保成功
Given 项目路径含空格/单引号，或启动命令投递失败/进程查询一直无法确认
When ensure
Then 命令参数正确转义；若无法确认真实持续值守运行则返回非0（不是只打印警告后0）。登记写入失败也必须失败，不能成功收尾；ensure锁按失败/中断清理，不留假健康状态。不新建服务/依赖。

对应补行为负例，至少对“忽略target继续复用”和“把cwd预检移回副作用之后”做反转验证，确认红项针对目标缺陷。仅改原冻结白名单，保留主控原始场景与失败证据，不改主仓源码，不commit/merge/push。完成后原会话报告，主控独立真实复验再限定审核。

working: 2026-09-15 scenario-02-watch 返修完成（三组真实反例，同线程）。
- user_切换主控不能复用错误目标：pane_probe 现在提取值守进程 argv 的 --pane 目标（输出 wake:<pid>@<target>），且 cmdline 含 --ensure/--check/--once/--dry-run 的短调用不算值守实例；ensure 复用前经 ensure_target_ok 复核——目标不符则非0拒绝并给「到该 pane Ctrl-C 后重跑 / 或 --pane 改回旧目标」步骤；候选 pane 查询失败（WATCH_QERR>0）一律拒绝新开实例。
- user_cwd预检失败不制造资源：--pane 预检上移到主控锁/显式修订写/worktree 建/账本写/agent start 之前；目标目录先推导不创建（--here=根、--worktree=给定、默认=.worktrees/<id>；不存在则向已存在祖先归一化物理路径再比对，绝不先建再判）；45e 验证默认 worktree 目标下拒绝时目录/分支/任务书字节均未变。
- user_启动没有确认就不能报确保成功：WATCH_CMD 改用 printf %q 逐参数转义（已实测含单引号路径 /tmp/qwb-s02-quote'-N 可正常 --dry-run 运行）；投递后连续探测仍确认不到值守进程 → 非0且不写 .watch；.watch 登记写入失败 → 非0；.watch.lock 失败/中断均清理（_ensure_body 返回后 rm -rf）。
测试：tests/smoke.sh 新增第45节 6 项（45a 目标不符拒复用+45a2 目标一致复用反转对照、45b 候选查询失败拒新开、45c 投递后不可确认非0不登记、45d 投递失败非0、45e cwd预检早于worktree创建、45f 预检早于修订写）；mk_proc 加第4参数注入值守 --pane 目标；stub 加 QWB_STUB_NOPROC 与 --pane 注入。
门结果：bash bin/qwb-test.sh fast → 退出码 0；bash bin/qwb-test.sh full → 退出码 0（SMOKE PASS 381 项 + LINT PASS）。无 commit/merge/push；本轮未开真实 herdr 窗口（真实复验留主控）。

## 主控独立复验与限定复审：仍不合并

- 原三组真实反例复验通过11项（真实Herdr、真实临时项目，无herdr/git替身）：首次与同目标ensure、切换目标拒绝、退出shell还在报未运行、停止后对新目标重启、cwd拒绝不产生分支/目录/锁且不改票、cwd拒绝不改修订基线、空格单引号路径真启动、登记写入失败非0。所有测试窗口与临时目录已清理。
- fast exit0 0.685s；full exit0 34.300s，共381条PASS（计数来自总输出，不冒充381个独立场景）。
- 额外真实信号试验：观察到.watch.lock创建后，向本次ensure进程组发SIGTERM；进程signalCode=SIGTERM，lockStillExists=true。清理了本轮专用资源。不能把函数返回后的rm称为中断清锁。
- Sol high限定复审（原生session 01a0a656-ca30-7563-b68e-6ce072395123）结论FAIL。Standards无影响本票的代码味道发现；Spec三项如下。前两个静态依据，第三项reviewer另做/tmp试验也得到rc143/LOCK_LEFT=YES。

### 第二轮返修（三点，不扩范围）
1. 本workspace身份边界：watch_candidates裸herdr pane list实测会列多个workspace（主控只读核对：21个pane，7个workspace）；必须显式限定调用者workspace，并在ensure任何副作用前确认目标pane确实存在且属于该workspace；登记旧pane在其它workspace不得认领/重启。原测试“跨workspace认领为正例”与本票规格冲突，要改成拒绝反例。不动别的workspace。
2. 混合不确定态：当前WATCH_FOUND成功分支在WATCH_QERR检查之前。一个活实例加一个查询失败候选时，仍会报确保成功，无法排除第二实例；scan错误/未知必须先于复用/新建成功分支处理，ensure fail-closed，check明确未知而不保证单实例。补“一个活 + 一个查询失败”的负例，不只测全失败。
3. 中断清锁：获取.watch.lock之后注册适当EXIT/INT/TERM清理，清理仅自己获得的锁；测试真实SIGTERM（必要时SIGINT）后锁不存在且下一次ensure可用，失败退出码保留。SIGKILL/断电无法捕获，明确为需核实后手工恢复的边界，不能假称全保证。不要借此加守护进程/抢锁超时。

审核窗口处理：发起审阅session 01a0a655-6e10-7780-b8c0-d9349e62880b误派了子审阅，主控已停止额外协调会话并直接接管上述Sol报告；没有再开新执行者。不再重复全仓审核，下次恢复实际审阅原线程仅核对这三点。

working: 2026-09-15 scenario-02-watch 第二轮返修完成（三点，不扩范围，同线程）。
- ① workspace 身份边界：watch_candidates/watch_scan 增加 workspace 参数，pane list 逐 pane 过滤 workspace_id，ensure 只在调用者 workspace（HERDR_WORKSPACE_ID 或经 HERDR_PANE_ID→pane get 推导）内行动；无 herdr 上下文直接拒绝。登记 pane 经 pane get 核实 workspace：属别的 workspace → 非0拒绝不认领不重启并给「核实后删 .watch 重跑 / 到该 workspace 手工处理」步骤；登记 pane 查询失败 → 非0拒绝；pane 不存在仍走 gone 重建。原「跨 workspace 认领」正例已反转为 42f 拒绝负例（登记 w8Z 的 pane + 其上真有值守进程 → rc=1，无 tab create/pane run）。
- ② 混合不确定态 fail-closed：_ensure_body 内 scanrc≠0 与 WATCH_QERR>0 已移到 WATCH_FOUND 成功分支之前——一个活实例加一个查询失败 → ensure rc=1 不复用不新开；watch_check 在发现活实例时若仍有查询失败 → 明确输出「值守：未知（…无法排除第二实例）」，不保证单实例。
- ③ 中断清锁：获 .watch.lock 后立即注册 EXIT/INT/TERM trap，只清本进程拿到的锁目录（QWB_WATCH_LOCKDIR 全局变量供 EXIT trap——函数返回后 local 已失效）；INT→130、TERM→143，原退出码经 trap 链保留；正常路径 rm+trap -。SIGKILL/断电无法捕获属「需人工核实后恢复」边界，已在脚本注释明确声明，不假称全覆盖。
测试：tests/smoke.sh mk_plist 支持 paneid@workspace 标注；42f 反转为跨 workspace 拒绝负例；新增第46节——46a 活实例+查询失败 ensure 拒绝、46a2 同布局 status 未知、46b 无查询失败正常复用（反转对照）、46c 真实 SIGTERM：持锁期间（stub QWB_STUB_SLOW_LIST=1 慢 list）kill -TERM → rc=143 锁已清、下一次 ensure rc=0。前一轮 11 条正确行为全部保留（42a-e/43/44/45 节未删改语义）。
调试记录：本轮一度误报 FAIL——statrun 未显式设 HERDR_WORKSPACE_ID，执行者 shell 身处真实 Herdr pane（w93）致 --check 按 w93 过滤；属测试环境泄漏非实现缺陷，statrun 已补显式 workspace。
门结果：bash bin/qwb-test.sh fast → 退出码 0；bash bin/qwb-test.sh full → 退出码 0（SMOKE PASS 386 项 + LINT PASS）。bash -n 与 shellcheck 均 0。
改动文件：bin/qwb-wake.sh（scan ws 过滤/QERR 前置/跨ws登记拒绝/信号trap）、tests/smoke.sh（mk_plist ws 标注/42f 反转/46节/statrun 显式 ws/stub QWB_STUB_SLOW_LIST）。未动主仓源码、03 worktree、全局工具；无 commit/merge/push；未开新真实 herdr 窗口。

## 第二轮交付的主控复验：只剩既有第1条的目标pane检查

- fast exit0（0.698s）；full exit0（40.606s，总输出386条PASS）。前轮真实Herdr 11项全部重跑通过。
- 真实SIGTERM：观察到锁后向独立ensure进程组发送SIGTERM，exit=143，lockLeft=false；下一次ensure真实启动exit0，中断清锁项通过。
- workspace扫描过滤及混合不确定态的错误分支已前置，源码和本轮相应smoke负例一致。
- 未完成的仍是上一节“第二轮返修”第1条：目标pane确实存在且属于该workspace。主控在真实Herdr临时项目执行 `qwb-wake.sh --project <本轮临时项目> --pane w93:nonexistent-s02-target --ensure`，实际exit=0，输出“已在本workspace新建值守tab并启动”，并写出了.watch。创建的值守pane=w93:p1M，根本不存在可接收提示的主控。
- 本轮临时项目和所建pane均已回收，无源代码修改。

### 最后一处返修（原要求，不增机制）
在watch_ensure中、获取.watch.lock以及扫描/启动/写登记之前，真实查询本次PANE：不存在、查询失败、缺workspace或不是调用者workspace都非0拒绝。不要只检查rp（登记的值守pane），也不要只检查PANE非空。
只补三条user正负例：不存在的目标拒绝且无tab/lock/.watch；异workspace目标拒绝且不碰对方pane；存在且同workspace目标仍可正常ensure。能真查则真查，不需要全仓再审。完成跑fast/full，保留现有386条行为，不commit/merge/push。随后主控只复验该缺口并恢复Sol原线程确认先前3审点收敛。

working: 2026-09-15 scenario-02-watch 第三轮补漏完成（主控复验指出的目标 PANE 核验缺口，不扩范围）。
- 缺口：此前只核了登记值守 rp 的 workspace，没核「本次要叫醒的 --pane PANE」本身——真实不存在的 w93:nonexistent-s02-target 曾被放行并建 watch。
- 修复：watch_ensure 在 ws 推导之后、获锁与一切副作用之前，对 PANE 做 pane get 核验：pane_not_found/查询失败 → 非0拒绝；无 workspace_id → 非0拒绝；workspace_id ≠ 调用者 ws → 非0拒绝。此刻 pane list 尚未调用、锁未获、无登记无 herdr 动作。
- 测试：ensreset 默认补 get-wtestctl（目标 pane 默认同 ws 存在，个别用例再覆盖）；46d 目标不存在 → rc=1 且零副作用（无 tab create/pane run/pane list、无 .watch、无 .watch.lock）；46e 目标属 w8Z → rc=1 零副作用；46f 同 ws 目标 → 正常建（对照）。前三轮全部用例语义未删改。
- 门结果：bash bin/qwb-test.sh fast → 退出码 0；bash bin/qwb-test.sh full → 退出码 0（SMOKE PASS 389 项 + LINT PASS）。
- 改动文件：bin/qwb-wake.sh、tests/smoke.sh。无 commit/merge/push；未开新真实 herdr 窗口。

---

## 验收记录（主控 / pi 会话，2026-09-15，第三轮后通过）

**执行链**：Devin SWE-2 Max 单线程三轮（实现→三组反例返修→ws边界/混合态/SIGTERM返修→目标pane补漏），原生 session `sincere-rocket`（w93:pM，已回收）。

**质量门（主控独立重跑，worktree 内）**

| 轮 | fast | full |
|---|---|---|
| 第 1 轮 | 0 | 0（374 PASS）——门过但场景不过，见第 1 轮反例 |
| 第 2 轮 | 0 | 0（381 PASS） |
| 第 3 轮（最终） | 0（0.715s） | 0（42.1s，**389 PASS / 0 FAIL** + LINT PASS） |

**主控独立复验（真实 Herdr + mktemp 临时项目，无任何 herdr/git 函数覆盖或 fixture 模拟；每轮试验窗口与临时目录均当场清理）**

- 前轮 11 项 live 场景每轮全部重跑：首次/同目标 ensure、切换主控目标拒绝、值守退出 shell 还在报未运行、停止旧值守后对新目标重启、cwd 拒绝零副作用（无 worktree/分支/锁/任务书字节变化）、cwd 拒绝不改修订基线、含空格+单引号路径真启动、引号路径真实健康、登记写入失败非 0 清锁 —— **全部 PASS**。
- 真实 SIGTERM：独立进程组观察到 `.watch.lock` 创建后发送 SIGTERM → exit=143、**锁已清**、下一次 ensure 真实启动 rc=0。
- 目标 pane 补漏（第三轮新增真实反例）：`--pane w93:nonexistent-s02-target` → **rc=1**「目标主控 pane 不存在…不擅自假定」且零副作用（无 tab/pane run/.watch/.watch.lock）；异 workspace 目标 `w8W:p5` → **rc=1**「属于 workspace w8W（当前 w93）」且零副作用、未触碰对方 pane。

**独立复审（Opus 5 / claude session 1dc67649-1df2-42ae-8f58-f11fcaeb67cc，w93:p1Y 已回收）**：前次 Sol 三项（跨 ws 泄漏 / 混合态 fail-open / SIGTERM 锁残留）在最终代码**全部收敛 PASS**；复审者另做**三次变异测试**（各删一项修复跑完整 smoke → 对应断言变红），证明测试针对行为而非文案。两条非阻塞观察：① mkdir 成功到 trap 装上之间微秒级无保护窗口（可达概率极低）；② 扫描后二次 pane_probe 返回 err/gone 时报错文案为「目标是 'err'」（fail-closed 仍正确，仅文案误导排障）。均不阻塞，留待后续顺手修。

**审核者变更说明**：原定 Sol 原线程恢复时 Codex 账户 Sol 额度耗尽（强制降级 Luna，已拒绝并关窗）；经使用者指定改用 **Opus** 完成本轮收敛核对。

**结论**：验收通过，`state: verified`。三轮门与真实复验证据齐备；残留非阻塞两项已登记。
