# Fable 终审：QW buddy 场景完善三票后能否交付

```
审核 id:  qwbuddy-final-fable
审核者:   claude-fable-5.1 / low（同上轮模型）
日期:     2026-09-15
抽查:     E2E-RUNBOOK、01/02/03 票全文、qwb-wake.sh、qwb-run.sh 预检段、qwb-lint.sh §7、
          tests/review-identity.sh、templates/TASK.md；scratch 真安装小实验 4 项（已清理）
```

## 结论表

| 审点 | 判定 | 一句话 |
|---|---|---|
| 1① 真实闭环 | **成立** | 人工介入逐条登记、"部分通过"不冒充 PASS，证据链齐 |
| 1② 解析收敛暂缓 | **理由不成立** | 围栏冲突源是模板自己；最小方案约 15 行且不碰票头 |
| 2 三票机制 | **与声明一致，无假绿** | 真实 herdr 复验 ensure 反例零副作用；lint §7 脏输入 fail-closed |
| 3 日常使用 | **可以** | 产品够了，流程过头已止涨；3 条最小缺口见下 |

## 论证

**1①。** RUNBOOK 把 trust 对话框吞提示词记为真实缺陷，Enter 与补发 prompt 都算人工介入，场景 1 判"部分通过"、场景 2 判"部分验证"，未验证边界单列。口径诚实。遗留：临时项目 `qwb-live.ZCBo9DXOVf` 仍在磁盘，01 票写明 02 复验后清理，未清。

**1②。** 02 票暂缓理由是"票头用围栏，粗暴过滤破坏协议"。核实：20 份票头确实把 `state:` 放围栏里，但我上轮建议的是**状态行**跳过围栏，不涉及 `state:` 头。冲突源就是 `templates/TASK.md`：§4 把 `blocked: spec-defect:`/`working: spec-resolved:` 示例放在围栏里，§5 把 `review-impl:` 示例放在围栏里。实验：照模板新建一票、工人一行未写，`qwb-status.sh` 已显示"最近: working: spec-resolved: <impl|spec>…"，值守指纹以它为基（静默污染）；保留 §5 示例再写真实两行，lint §7 取到示例行报"同家族(<模型家族>)"（fail-closed，不假绿但误导）。**判定：应做，不必等单独设计。** 方案：一个 `status_lines <file>`（awk 跳围栏后 grep 四前缀），四处 grep 换用；`state:`/`review-*:` 头部解析不动；模板示例改缩进块。约 15 行 + 2 断言。

**2。** `--ensure`：目标 pane 核验先于锁与扫描、混合态先于成功分支、trap 只清自己的锁，与三轮返修声明一致；真实 herdr 下不存在目标与异 workspace 目标均 rc=1、无 .watch/.lock。`--pane` 预检先推导目录再比物理路径，位于一切副作用前，一致。脆弱点一处：pane_probe 用 perl 解析 process-info argv 提取值守目标，依赖 herdr 字段形状，fixture 为 0.8.2 真录，升级 herdr 时是第一个要看的地方。lint §7：只查结构、不猜型号、unknown 与缺字段皆 FAIL，与声明一致；`review-required: Yes` 大写会被当普通票跳过，是宽松不是假绿。模板 §5 与场景冻结/疑点门前缀不同、不冲突。

**3。** 只提需求（--ensure 由主控拉值守）、零通知、可见执行、跨会话续接、可审计五条成立；主控退出后由使用者重启已明示不保证。成本：运行时薄，自审记账三票后未再增。**可以开始日常使用。** 最小缺口：

1. 状态行解析跳过围栏 + 模板示例去围栏。不修，第一张按模板新建的票就污染 status 与指纹。
2. `templates/QWBUDDY.md` 补 2 行："新项目首次派发前先在工人 tab 手工受信任一次"（trust 现象目前只在 RUNBOOK）。
3. 清理 `qwb-live.ZCBo9DXOVf`，"临时项目谁建谁清"写进 lessons。

总评：产品够了，流程过头的部分已止涨；先修缺口 1 再上真实项目。

FINAL AUDIT DONE 通过 3
